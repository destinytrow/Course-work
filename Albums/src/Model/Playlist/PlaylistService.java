package Model.Playlist;

import Model.DatabaseConnection;

import java.util.List;


class PlaylistService {
    public static void selectAll(List<Playlist> targetList, DatabaseConnection database) {

    }	public static Playlist selectById(int playlistID, DatabaseConnection database) { return null; }
    public static void save(Playlist playlist, DatabaseConnection database) { }
}
